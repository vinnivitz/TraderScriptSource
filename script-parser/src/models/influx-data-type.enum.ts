export enum INFLUX_ENTITY_TYPE {
  BUCKETS = 'buckets',
  TASKS = 'tasks',
  CHECKS = 'checks',
  NOTIFICATION_ENDPOINTS = 'notificationEndpoints',
  NOTIFICATION_RULES = 'notificationRules',
  TELEGRAFS = 'telegrafs'
}
